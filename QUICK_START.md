# 🚀 ШВИДКИЙ ГАЙД - Як скопіювати в свій проект

## Варіант 1: Замінити існуючий проект

```bash
# 1. Видали старий проект (зроби backup якщо треба!)
cd D:/RustPjt
rm -rf LoggerLikeSlog  # або перейменуй в LoggerLikeSlog_old

# 2. Створи нову папку і скопіюй файли
mkdir LoggerLikeSlog
cd LoggerLikeSlog

# 3. Скопіюй ВСІ файли з logger_workspace/ сюди
# (структуру збережи!)
```

## Варіант 2: Створити поруч

```bash
cd D:/RustPjt

# Створи нову папку
mkdir SampleLoggerWorkspace
cd SampleLoggerWorkspace

# Скопіюй всі файли з logger_workspace/ сюди
```

## 📋 Контрольний список файлів

Переконайся що скопіював:

```
✅ Cargo.toml                           (workspace root)
✅ README.md
✅ logger/
   ✅ Cargo.toml
   ✅ src/lib.rs
   ✅ src/sub_func.rs
✅ logger_derive/
   ✅ Cargo.toml
   ✅ src/lib.rs
✅ test_app/
   ✅ Cargo.toml
   ✅ src/main.rs
```

## 🧪 Тестування після копіювання

```bash
# В корені workspace (там де головний Cargo.toml)
cargo build

# Якщо все ок - запускаємо тест
cd test_app
cargo run
```

## ✅ Очікуваний вивід

```
=== Тестування логера ===

[DEBUG] : 26-01-06 10:30:45 -> Це debug повідомлення
[INFO ] : 26-01-06 10:30:45 -> Це info повідомлення
[ERROR] : 26-01-06 10:30:45 -> Це error повідомлення

[EVENT] : 26-01-06 10:30:45 -> Когенератор запустився
[EVENT] : 26-01-06 10:30:45 -> Температура досягла 85°C
[WARN ] : 26-01-06 10:30:45 -> Низький рівень палива
[WARN ] : 26-01-06 10:30:45 -> Перевантаження по струму
[CRITICAL] : 26-01-06 10:30:45 -> АВАРІЙНА СИТУАЦІЯ!

=== Тест з багатьох потоків ===

[EVENT] : 26-01-06 10:30:45 -> Потік 0 - повідомлення 0
[EVENT] : 26-01-06 10:30:45 -> Потік 1 - повідомлення 0
...
```

## 🔥 Якщо щось не компілюється

1. **Перевір що всі файли на місці** (контрольний список вище)
2. **Перевір що ти в корені workspace** (`ls` має показати logger/, logger_derive/, test_app/)
3. **Спробуй cargo clean && cargo build**
4. **Подивись помилку** - напиши мені, виправимо

## 📝 Наступні кроки

Після того як все запрацює:

1. Видали `test_app/` якщо не треба
2. Використовуй `logger/` в своїх проектах:
   ```toml
   [dependencies]
   sample_logger = { path = "../logger" }
   ```
3. Опублікуй на GitHub (опціонально)

## 🍺 Якщо все зламалось

Напиши мені:
- Що саме не працює
- Яку помилку видає
- Структуру папок (`tree` або `ls -R`)

Виправимо разом! 💪
